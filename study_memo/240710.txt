증감연산자
논리연산자
비교연산자

>
<
>=
<=

JS > 변수 안에 데이터 값을 넣음
그 데이터 값은 타입을 갖음
타입 => 문자열 / 숫자 / 논리형 / 객체 / 함수 / 배열 등의 종류가 있음

== : 같다.의 의미(느슨한 비교)
1 "1" > 따옴표가 붙으면 문자
1 == "1" (같다)

=== : 같다.의 의미(엄격한 비교)
1 === "1" (다르다)

!== > 긍정의 반대를 의미, 즉 "다르다"
1 !== "1" (다르다)